version: "3.8"

services:
  app:
    build: .
    ports:
      - "5177:5177"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_OPENAI_API_KEY=${OPENAI_API_KEY:-}
    command: npm run dev -- --host 0.0.0.0 --port 5177
